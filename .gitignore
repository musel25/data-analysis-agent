# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Notebooks
.ipynb_checkpoints/

# Agent runtime artifacts (the LLM response cache IS committed — see DECISIONS.md D12)
runs/
figures/

# Agent-generated outputs (the executor writes to CWD — see D05)
*_audit.csv
grouped_*.csv

# Figures the agent writes to CWD
*.png

# eval runtime lock
evals/results.lock

# LaTeX build artifacts (docs/slides)
docs/slides/.out/
docs/slides/handout.tex
